# Global configuration & runtime flags
CONFIG = {
    "dry_run": True,            # first run safety switch
    "account_no": "YOUR_ACC_NO",  # Kiwoom 계좌 (모의 가능)
    "screen_no": "1000",
    "budget": 10_000_000,
    "max_exposure": 0.3,
    "day_dd_limit": 0.05,
}
